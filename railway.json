{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'set -e; echo \"[boot] migrate\"; prisma migrate deploy; echo \"[boot] env\"; env | grep -E \"^(PORT|HOSTNAME|NODE_ENV|AUTH_URL)=\"; echo \"[boot] starting node\"; exec node server.js'",
    "healthcheckPath": "/",
    "healthcheckTimeout": 60,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
